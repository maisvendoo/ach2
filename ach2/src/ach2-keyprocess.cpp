#include    "ach2.h"

//------------------------------------------------------------------------------
//
//------------------------------------------------------------------------------
void ACH2::keyProcess()
{
    // АЗВ "Управление"
    if (getKeyState(KEY_J))
    {
        if (isShift())
            azv_control.set();
        else
            azv_control.reset();
    }
}
